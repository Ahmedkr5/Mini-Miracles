{% extends 'base.html.twig' %}

{% block body %}
<div style="width: 100%; overflow: hidden;">
    <div style="width: 200px; float: left;">
    {% for g in grpnom %}
        <h1>Groupe {{ g.nomGroup }} </h1>
    {% endfor %}
    </div>
    <div style="margin-left: 100px;margin-top: 33px;">
    {% for n in num %}
        <h4 style="color: #ce8483">({{ n.1 }} Enfants)</h4>
    {% endfor %}
    </div>
</div>




    <br><br>
    <table   class="table table-hover  " style="background-color: white;">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Afficher</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for e in grouplist %}
            <tr>
                <td>{{ e.nom }}</td>
                <td>{{ e.prenom }}</td>


                <td>


                    <a href="{{ path('enfant_show', { 'id': e.id }) }}" class= "fa fa-user" style="color: #ce8483;"></a>

                </td>
                <td>


                    <a href="{{ path('enfant_edit', { 'id': e.id }) }}"class= "fa fa-trash" style="color: #AF0000"></a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table><br><hr>
    <a href="{{ path('groupe_index') }}" class="btn btn-primary">Retour</a>


{% endblock %}
